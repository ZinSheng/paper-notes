# GlobalDentBench example

This self-contained example includes the reading dashboard and the GlobalDentBench paper page.

Open `dashboard.html` in a browser, then select the paper card. The example bundles local fonts and figure images; MathJax is loaded from its CDN when formulas are present.
